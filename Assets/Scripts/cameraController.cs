using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cameraController : MonoBehaviour
{
    // want to have camera move slowly around world as generations progress
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
